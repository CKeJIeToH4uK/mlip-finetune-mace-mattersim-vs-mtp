# Questions For User

1. For final slides, should the H2O MTP row be labeled `MTP level 16` with a footnote about misleading `mtp20` filenames/job names?
2. Should final result tables include total energy errors (`Energy`, eV) for MTP, or only `Energy/Atom` and `Forces` for consistency with MACE/MatterSim?
3. Should `loss_mace.png` and `loss_mattersim.png` go into the main deck, or should they wait for a separate training-log normalization step?
4. Should the all-in-one figures (`mae_energy_all.png`, `mae_forces_all.png`, `rmse_energy_all.png`, `rmse_forces_all.png`) be used as overview slides, or should H2O/alloy be shown only with per-dataset figures?
5. For final presentation units, should force errors be shown in eV/A or converted to meV/A for all models?
