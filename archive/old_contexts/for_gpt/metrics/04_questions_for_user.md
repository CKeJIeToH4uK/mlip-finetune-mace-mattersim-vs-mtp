# Questions For User

1. For initial zero-shot rows, should the split be called `valid`, `test`, or `validation/test` in final slides? The scripts use `valid_path`, but the actual file is `test_300K.xyz`.
2. Should the very large initial zero-shot energy-per-atom errors around 647 eV/atom be shown in the main deck, or only mentioned with an energy-offset caveat?
3. For force plots/tables, do you prefer eV/A or meV/A in the final presentation?
4. Are the PNG figures in `19.03.26` and `repo/coursework/new_datasets/figures/cross_temp_organic.png` acceptable as presentation figures without recovering the exact plotting script?
5. Should MTP `initial-random` be included in the final talk, or only used as internal sanity-check evidence for the initial evaluation stage?
